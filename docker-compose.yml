services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: micromega
      POSTGRES_USER: micromega
      POSTGRES_PASSWORD: micromega
    ports:
      - "5432:5432"
    volumes:
      - micromega_pgdata:/var/lib/postgresql/data

volumes:
  micromega_pgdata:
